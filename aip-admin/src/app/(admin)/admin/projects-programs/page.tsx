import ProgramsTable from "../../_shared/components/programs-projects/PageTable";
export const dynamic = "force-dynamic";
const Add: React.FC = async () => {
  return <ProgramsTable />;
};

export default Add;
